# Продакшен-сборка (npm run build) для выкладки на vue.cabrioride.by/app.
# Backend (PHP API + тестовая БД host1708875_cabrioridedev) остаётся на dev.cabrioride.by —
# отдельный домен, поэтому на backend нужно разрешить CORS для https://vue.cabrioride.by.
VITE_API_URL=https://dev.cabrioride.by/app/backend
