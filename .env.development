# Локальная разработка (npm run dev).
# Пока указывает на локальный backend старого приложения (XAMPP) — можно переключить
# на dev.cabrioride.by, если тестируешь через реальный тестовый сервер.
VITE_API_URL=http://localhost/Cabrio_app/backend
